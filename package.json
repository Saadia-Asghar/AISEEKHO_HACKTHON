{
  "name": "khidmat-ai",
  "private": true,
  "scripts": {
    "build": "npm run vercel-build",
    "vercel-build": "npm install --prefix khidmat-ai/mobile && npm run build:web --prefix khidmat-ai/mobile"
  }
}
